version: '3.7'
services:
  e7_in_docker:
   build: .
   environment:
    - PORT=8081
    - REDIS_HOST=redis
    - REDIS_PORT=6379
   ports:
    - 8081:8081
   depends_on:
    - redis
    - mongo
  redis:
    image: redis:6.0.6-alpine
    command: redis-server
  mongo:
    image: mongo:latest


